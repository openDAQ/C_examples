#include <read_in_loop.h>

void setup(daqInstance* instance, daqList** availableSignals, daqStreamReader** streamReader)
{

}

void reading(daqStreamReader* reader, daqSignal* signal, daqUInt duration)
{
    
}
