#include <read_in_loop.h>

void convert_timestamps()
{
    // Get reference domain info from the device itself, or is there another origin point of this data ???

    // Then I also need to check if the everything crashes if the timeprotocol is changed

    // Also ask about the converesions between the 
}
